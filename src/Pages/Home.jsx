import { Navbar, Hero, ArticlesSection, Footer } from '../Components/index';

const Home = () => {
    return (
        <div>
            <Navbar />
            <Hero />
            <ArticlesSection />
            <Footer />
        </div>
    );
};

export default Home;
